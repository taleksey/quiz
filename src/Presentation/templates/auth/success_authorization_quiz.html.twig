{% extends 'base.html.twig' %}
{% block title %} Authorization Success{% endblock %}

{% block header %}
    {{ include('base/header.html.twig', {isAuthorizePage: true}) }}
{% endblock %}
{% block body %}
    <!-- Begin page content -->
    <main class="flex-shrink-0 main">
        <div class="container mt-5">
            <h2>You have authorized successfully.</h2>
        </div>
        <div class="container">
            <div class="col-md-10 col-lg-10">
                <div class="border">
                    <div class="question bg-white p-3 border-bottom">
                        <div class="d-flex flex-row justify-content-between align-items-center mcq">
                            <p class="authorization-success">You can create new quiz now.  You can do it click on link <a href="{{ path('quiz_new') }}">Link on creating new quizzes</a>
                                or select menu "Add newQuiz"</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
{% endblock %}
